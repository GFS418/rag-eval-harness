# Runtime deps for the deployed Streamlit app. This is the file Streamlit
# Community Cloud installs, so it is kept lean and pins CPU-only PyTorch
# (the default Linux wheel pulls ~2 GB of CUDA libraries).
# Full project environment (evaluation, fine-tuning, tests): requirements-project.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4
sentence-transformers>=3.0
transformers>=4.40
streamlit>=1.35
numpy>=1.26
pandas>=2.2
pyarrow>=15
bm25s>=0.2
anthropic>=1.0
huggingface_hub>=0.23
