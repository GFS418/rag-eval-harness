# Full project environment (pipeline, evaluation, fine-tuning).
# The deployed Streamlit app uses requirements.txt (leaner).
torch>=2.4
sentence-transformers>=3.0
transformers>=4.40
bm25s>=0.2
numpy>=1.26
pandas>=2.2
pyarrow>=15
pyyaml>=6.0
tqdm>=4.66
anthropic>=1.0
pytest>=8.0
accelerate>=1.1.0
